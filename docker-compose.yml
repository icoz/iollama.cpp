version: '3.8'
services:
  iollama:
    build: .
    ports:
      - "8080:8080"
    environment:
      IOLLAMA_MODEL_PATH: /models/model.gguf
      IOLLAMA_HOST: 0.0.0.0
    volumes:
      - ./models:/models
    restart: unless-stopped
